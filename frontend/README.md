# Frontend

The Vite React app lives in **`frontend/`** (this folder).

```bash
cd frontend
npm install
cp .env.example .env
npm run dev
```

See [frontend/README.md](frontend/README.md) for details.
